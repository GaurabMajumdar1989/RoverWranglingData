def abort(chunks: list[dict], query: str) -> list[dict]:
    # Placeholder implementation for abort logic based on query relevance 
  print("ABORT ABORT ABORT !!!!!!...")
  return [{} for _ in chunks]  # Return empty dicts as placeholders